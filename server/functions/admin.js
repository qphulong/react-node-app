const { currentUser } = require("../app");
const Post = require("../models/post");
const bcrypt = require("bcrypt");

async function giveModeratorJob(userId) {

}

module.exports = {
    giveModeratorJob,
};